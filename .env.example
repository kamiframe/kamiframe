# Copy to .env and fill in, if you ever need one. .env is gitignored.
#
# Nothing needs this today. Pixellab is reached through its MCP server,
# whose credential lives in Claude Code's own config (`claude mcp add`),
# not here -- one copy of a key is safer than two.
#
# This file exists for the case where a tool under tools/ needs to talk to
# something directly, without an MCP server in front of it.

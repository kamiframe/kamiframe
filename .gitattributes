# Normalise line endings so a Windows checkout and a Linux CI run see the
# same bytes. Without this, CI can fail on files that look identical.
* text=auto eol=lf

*.png binary
*.jpg binary
*.gif binary
*.wav binary
*.bin binary
*.kfa binary
